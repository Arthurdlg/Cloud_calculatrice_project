apiVersion: v1
kind: Service
metadata:
  name: calculatrice-service
spec:
  selector:
    app: calculatrice-service
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP

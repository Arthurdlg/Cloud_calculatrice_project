apiVersion: v1
kind: Service
metadata:
  name: calculatrice-front
  labels:
    app: calculatrice-front
spec:
  type: LoadBalancer
  selector:
    app: calculatrice-front
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
